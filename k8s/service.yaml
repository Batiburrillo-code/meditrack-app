apiVersion: v1
kind: Service
metadata:
  name: meditrack-api-service
  namespace: meditrack-dev
spec:
  type: LoadBalancer
  selector:
    app: meditrack-api
  ports:
  - port: 80
    targetPort: 3000
